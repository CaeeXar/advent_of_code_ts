export type SnailNumber = {
    value?: number,
    left?: SnailNumber,
    right?: SnailNumber,
};

const createNumbers = (line: string, depth: number): SnailNumber => {
    return {};
};

export const parsePartOne = (input: string) => {
    let lines = input.split("\n"), tree: SnailNumber = {};
    return 0;
};