export const parsePartOne = (input: string) => {
  return input.split("\n").map((d) => d.trim());
};

export const parsePartTwo = (input: string) => {
  return input.split("\n").map((d) => d.trim());
};
